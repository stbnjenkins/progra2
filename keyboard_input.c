
void processNormalKeys(unsigned char key, int x, int y) {
	switch(key){
        case 27: //escape
            exit(0);
        case 82: // R
            printf("Refrescando escena a partir del archivo\n");
            // reload image
            return;
        case 114: // r
            printf("Refrescando escena a partir del archivo\n");
            // reload image
            return;        
    }
}
